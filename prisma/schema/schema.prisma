datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
}

/**
 * users have many accounts.
 * accounts have many categories and transactions.
 * categories belong to an account and can have many transactions.
 * transactions belong to an account and optionally to a category.
 */
