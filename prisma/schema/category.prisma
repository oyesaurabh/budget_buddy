model categories {
    id          String         @id @default(cuid())
    name        String
    user_id     String
    user        users          @relation(fields: [user_id], references: [id])
    transaction transactions[]
}
